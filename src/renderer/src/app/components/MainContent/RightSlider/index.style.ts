import { createStyles } from 'antd-style'

const useRightSliderStyle = createStyles(() => {
  return {
    rightSlideBarCloser: {
      textAlign: 'right'
    }
  }
})
export default useRightSliderStyle
